// enums
export enum eUSER_ROLE {
    HOTEL = 'hotel',
    GUEST = 'guest',
    //OWNER = 'owner',
}


//--- Room
export enum eCOLOR {
    BLUE = 'blue',
    GREEN = 'green',
    WHITE = 'white',
}

export enum eROOM_SIZE {
    SINGLE = "single",
    DOUBLE = "double",
}
