export enum ROLE_ENUM {
  hotel = 'hotel',
  guest = 'guest',
}

export enum KIND_OF_ROOM_ENUM {
  single = 'single',
  double = 'double',
}

export enum GENDER_ENUM {
  male = 'male',
  female = 'female'
}

export enum METHODS_REQUEST_ENUM {
  GET = 'GET',
  POST = 'POST',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}

export enum ROOM_FILTER {
  available = "available",
  booked = "booked",
  cleaned = "cleaned",
  notCleaned = "notCleaned",
  single = "single",
  double = "double",
}
