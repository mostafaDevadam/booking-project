export enum METHOD {
    'GET' = 'GET',
    'POST' = 'POST',
    'PATCH' = 'PATCH',
    'DELETE' = 'DELETE',
}


export enum ROLE_USER_ENUM {
    hotel = 'hotel',
    guest = 'guest',
}


export enum KIND_OF_ROOM_ENUM {
    single = 'single',
    double = 'double',
}
